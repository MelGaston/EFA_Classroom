let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days); // Array
console.log(days.toString());
console.log(days.length);

let toString

let tired = true; // Boolean

let soup = {
    a:"chicken noodle",
    b:"tomato",
    c:"beef and barley"
}

console.log(soup.a); // Object

let car = "Honda"; // String

let dec = 10.2 // Float

let num = 8 // Integer